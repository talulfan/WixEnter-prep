/**
 * Exercise: Counter Component (React Fundamentals)
 *
 * Build a component `Counter` that:
 * - Accepts props `initialCount` (default 0) and `step` (default 1).
 * - Displays the current count.
 * - Has buttons to subtract step, add step, and reset to `initialCount`.
 * - Uses functional updates when the new count depends on the previous count.
 *
 * Export: `export function Counter({ initialCount = 0, step = 1 }) { ... }`
 */

import { useState } from 'react';

export function Counter({ initialCount = 0, step = 1 }) {
  // YOUR CODE HERE
}
