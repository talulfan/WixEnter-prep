/**
 * LeetCode #167: Two Sum II - Input Array Is Sorted
 * https://leetcode.com/problems/two-sum-ii-input-array-is-sorted/
 *
 * Given a 1-indexed array of integers `numbers` that is already sorted in
 * non-decreasing order, find two numbers such that they add up to a specific
 * `target` number. Return indices in 1-indexed format [index1, index2].
 *
 * Exactly one solution exists. You may not use the same element twice.
 *
 * Examples:
 *   twoSum([2, 7, 11, 15], 9)  → [1, 2]
 *   twoSum([2, 3, 4], 6)       → [1, 3]
 *   twoSum([-1, 0], -1)        → [1, 2]
 *
 * Goal: O(n) time, O(1) space — two pointers on sorted array.
 */

function twoSum(numbers, target) {
  // YOUR CODE HERE
}

console.log(twoSum([2, 7, 11, 15], 9));   // [1, 2]
console.log(twoSum([2, 3, 4], 6));        // [1, 3]
console.log(twoSum([-1, 0], -1));         // [1, 2]
