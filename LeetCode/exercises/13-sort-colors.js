/**
 * LeetCode #75: Sort Colors
 * https://leetcode.com/problems/sort-colors/
 *
 * Difficulty: Medium
 * Suggested time: ~20 minutes
 *
 * Given an array `nums` with values 0, 1, 2 (red, white, blue), sort them in-place
 * so that equal colors are grouped in order 0, 1, 2. Use one-pass if possible.
 *
 * Example: sortColors([2,0,2,1,1,0]) → [0,0,1,1,2,2]
 *
 * Topics: two pointers, Dutch national flag, sorting
 */

function sortColors(nums) {
  // YOUR CODE HERE
}

const a = [2, 0, 2, 1, 1, 0];
sortColors(a);
console.log(JSON.stringify(a)); // [0,0,1,1,2,2]
