/**
 * Exercise: Todo List Component (React Fundamentals)
 *
 * Build a component `TodoList` that:
 * - Adds todos from a controlled input (form submit; trim empty text).
 * - Each todo: `{ id, text, done }` with stable numeric `id`.
 * - Toggles done via checkbox; deletes with a button.
 * - Filters: All / Active / Completed.
 * - Shows count of items left (not done).
 * - Uses `key={id}` on list items (not array index).
 *
 * Export: `export function TodoList() { ... }`
 */

import { useState } from 'react';

export function TodoList() {
  // YOUR CODE HERE
}
