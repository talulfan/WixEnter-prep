/**
 * Exercise: Greeting Component (React Fundamentals)
 *
 * Build a component `Greeting` that:
 * - Accepts an optional prop `title` (default: `'Welcome'`).
 * - Renders a controlled text input for the user's name.
 * - Shows `Hello, {name}!` when name is non-empty; otherwise `Hello, guest!`
 * - Uses `useState` and a semantic `<label htmlFor="...">` linked to the input.
 *
 * Export: `export function Greeting({ title = 'Welcome' }) { ... }`
 */

import { useState } from 'react';

export function Greeting({ title = 'Welcome' }) {
  // YOUR CODE HERE
}
